# Monitoring stack for management cluster
defaultNamespace: cattle-monitoring-system

# Clean up resources when bundle removed
keepResources: false

# Target management cluster only
targetCustomizations:
- name: management-only
  clusterSelector:
    matchLabels:
      env: management
