# Fleet Configuration for Cluster Services
# Targets: Management cluster only (env=management)

# Target namespace for deployment
namespace: kube-system

# Target management cluster
targets:
- name: management-cluster
  clusterSelector:
    matchLabels:
      env: management
