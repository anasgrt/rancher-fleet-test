# Cluster-wide services for management cluster
defaultNamespace: kube-system

# Clean up resources when bundle removed
keepResources: false

# Target management cluster only
targetCustomizations:
- name: management-only
  clusterSelector:
    matchLabels:
      env: management
