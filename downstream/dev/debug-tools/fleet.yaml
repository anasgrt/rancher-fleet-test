# Fleet Configuration for debug-tools
# Deploys ONLY to dev clusters using targetCustomizations

defaultNamespace: debug-tools
keepResources: false

targetCustomizations:
- name: dev
  clusterSelector:
    matchLabels:
      env: dev
