# Fleet bundle configuration
defaultNamespace: fleet-test

# Target DOWNSTREAM clusters only
targetCustomizations:
- name: downstream-clusters
  clusterSelector:
    matchLabels:
      cluster-type: downstream
