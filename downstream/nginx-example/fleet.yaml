# Fleet Configuration for nginx-example
# Simple test application for downstream clusters

# Target namespace for deployment
namespace: fleet-test

targetCustomizations:
- name: all-downstream
  clusterSelector:
    matchExpressions:
    - key: env
      operator: In
      values:
      - dev
      - prd
