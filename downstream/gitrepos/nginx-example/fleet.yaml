# Simple nginx test application
defaultNamespace: fleet-test

# Clean up resources when bundle removed
keepResources: false

# Target all downstream clusters (dev and prd)
targetCustomizations:
- name: all-downstream
  clusterSelector:
    matchExpressions:
    - key: env
      operator: In
      values:
      - dev
      - prd
