# Fleet Configuration for nginx-ingress Controller
# Targets: Downstream clusters based on environment labels

helm:
  # External Helm chart reference
  chart: ingress-nginx
  repo: https://kubernetes.github.io/ingress-nginx
  version: 4.11.3
  releaseName: nginx-ingress
  
  # Helm release behavior - OPTIMIZED FOR VAGRANT/BARE-METAL
  wait: false               # Don't wait for LoadBalancer (speeds up deployment)
  timeoutSeconds: 600       # Extended timeout for initial deployment
  
  # Values override file
  valuesFiles:
  - values.yaml

# Target clusters with env label (dev or prd)
# This deploys to downstream1 (env:dev) and downstream2 (env:prd)
targetCustomizations:
- name: all-downstream
  clusterSelector:
    matchExpressions:
    - key: env
      operator: In
      values:
      - dev
      - prd
