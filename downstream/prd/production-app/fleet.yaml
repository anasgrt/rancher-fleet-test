# Fleet Configuration for production-app
# Targeting: Controlled by GitRepo 'prd' which targets clusterSelector: env=prd
# This bundle deploys ONLY to prd clusters via GitRepo-level targeting

defaultNamespace: production-app
keepResources: false
