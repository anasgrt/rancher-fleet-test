# Fleet Configuration for production-app
# Deploys ONLY to prd clusters using targetCustomizations

defaultNamespace: production-app
keepResources: false

targetCustomizations:
- name: prd
  clusterSelector:
    matchLabels:
      env: prd
